-- Criação da base dos dados

CREATE DATABASE formulario;

-- Usando a base de dados criada;

USE formulario;

-- Criando uma tabela dentro do formulario

CREATE TABLE dados(
	nome VARCHAR(40),
	age INT,
    coment VARCHAR(80)
);